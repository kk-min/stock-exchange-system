package menu;
/**
 * Interface that communicates with other classes for printing different menus into the terminal.
 * @author  Min
 * @version 1.0
 * @since   2022-01-18
 */
public interface Printable {
    /**
     * Prints something into the terminal.
     */
    void print();
}
